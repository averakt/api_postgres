# Репозиторий для обновления структуры БД и запуска БД

## Как запустить

Run

````
$ docker-compose --compatibility up -d --build
````

Использование `--compatibility` чтобы имя приложения docker имело имя с подчеркиваниями. 